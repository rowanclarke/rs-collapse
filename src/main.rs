#![feature(
    type_alias_impl_trait,
    return_position_impl_trait_in_trait,
    impl_trait_projections
)]

/*
mod collapse;
mod grid;
mod iter;
mod puzzle;
mod sudoku;

use puzzle::{Solver, Puzzle};
use collapse::{Collapse, FromCollapse};

fn main() {
    let mut solver = Solver::new();
    let solution = solver.solve(None).unwrap();
    println!("{}", Puzzle::from_collapse(solution));
}
*/

fn main() {}
